import { describe, it, expect } from 'vitest';

// TODO: add real auth service tests (login, register, password reset)
describe('authService (placeholder)', () => {
  it('initial placeholder', () => expect(true).toBe(true));
});
